# Framework cache export collector retrigger 2

Docs-only later main push for the credential-free Server Bridge result collector after both framework cache export tasks have had a full execution interval. No production behavior changes.
